# playwright-fill-issue

Playwright fails in trying to call `.fill()` method for input with `type="date"`

Example allows to reproduce failed test and possible workaround

Just run:

* `yarn install`
* `yarn test`

And check the report